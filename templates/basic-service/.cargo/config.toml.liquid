# JAM Service Build Configuration
# The exact RISC-V target depends on the PolkaVM/JAM ecosystem
# See: https://github.com/paritytech/polkavm for current target specs

[build]
# target = "riscv32em-unknown-none-elf"  # Uncomment when target is available

[unstable]
build-std = ["core", "alloc"]
build-std-features = ["panic_immediate_abort"]
